import { Coords, MapOptions, Props } from "google-map-react";

export const DEFAULT_CENTER: Coords = {
  lat: 42.9784587,
  lng: -78.8669686,
};

export const DEFAULT_ZOOM = 13;

export const MAP_OPTIONS: MapOptions = {
  mapTypeId: "terrain",
};

export const BREAKPOINT_DESKTOP = 800;
